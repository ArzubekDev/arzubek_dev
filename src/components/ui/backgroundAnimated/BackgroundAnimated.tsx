import React from 'react';
import scss from "./BackgroundAnimated.module.scss"

const BackgroundAnimated = () => {
    return (
        <div className={scss.container}>
            
        </div>
    );
};

export default BackgroundAnimated;